import{isNullOrWhiteSpaces}from"./general.js";export default class ScrollController{constructor(e){if(isNullOrWhiteSpaces(e.scrollButtonsSelector))throw new Error("[SCROLL-ELEMENTS] Incorrect scroll-buttons selector!");let l=document.querySelectorAll(e.scrollButtonsSelector);for(let o of l)o.addEventListener("click",(()=>this.scrollToElement(o)));if(0==isNullOrWhiteSpaces(e.fixedHeaderSelector)){let l=document.querySelector(e.fixedHeaderSelector).clientHeight;ScrollController.fixedHeaderHeight=l}}scrollToElement(e){let l=document.querySelector(e.dataset.scrollTo);if(null==l)throw new Error("[SCROLL-ELEMENTS] Something wrong with scrollElement!");let o=window.pageYOffset+l.getBoundingClientRect().top;window.scrollTo({top:o-ScrollController.fixedHeaderHeight,behavior:"smooth"})}}ScrollController.fixedHeaderHeight=0;